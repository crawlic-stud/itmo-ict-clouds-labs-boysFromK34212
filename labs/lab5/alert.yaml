alertmanager:
  config:
    global:
      resolve_timeout: 5m
      telegram_api_url: "https://api.telegram.org"

    route:
      receiver: telegram

    receivers:
      - name: telegram
        telegram_configs:
          - chat_id: 797055891
            bot_token: "7702757976:AAGShoTC_EjxBnpf8lBOOtqrJR1eM2u3LaQ"
            api_url: "https://api.telegram.org"
            send_resolved: true
            parse_mode: Markdown
            message: |-
              {{ range .Alerts }}
                🚨 *{{ .Annotations.summary }}*
                {{ .Annotations.description }}
                *Details:*
                {{ range .Labels.SortedPairs }} • *{{ .Name }}:* `{{ .Value }}`
                {{ end }}
              {{ end }}

serverFiles:
  alerting_rules.yml:
    groups:
      - name: nginx_alerts
        rules:
          - alert: NginxDown
            expr: nginx_up == 0
            labels:
              severity: critical
            annotations:
              summary: "Nginx is down on {{ $labels.instance }}"
              description: "No response from Nginx exporter for more than 1 minute."
        
          - alert: HighHttpRequests
            expr: nginx_http_requests_total > 1000
            for: 5m
            labels:
              severity: critical
            annotations:
              summary: "HTTP requests exceed 1000 on Nginx instance {{ $labels.instance }}"
              description: "The total number of HTTP requests on Nginx has exceeded 1000 for the last 5 minutes."
